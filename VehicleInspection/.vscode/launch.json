{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Test Debugging",
      "command": "cds watch --profile hybrid",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}