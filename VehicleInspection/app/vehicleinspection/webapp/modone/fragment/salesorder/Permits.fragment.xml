<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">

    <HBox>
        <VBox width="100%">
            <Table id="idPermitTable" mode="MultiSelect" inset="false" items="{
                path: 'EntryFormDataSourceModel>/Permits',
                sorter: [{ path: 'pinned', descending: true }],
                groupHeaderFactory: '.getGroupHeader'
                 }">

                <columns>
                    <!-- Title Column -->
                    <Column width="10rem">
                        <Text text="Permits No"/>
                    </Column>
                    <Column width="10rem">
                        <Text text="Permits Type"/>
                    </Column>

                    <!-- Description Column -->
                    <Column width="10rem">
                        <Text text="Permits Type in Arabic"/>
                    </Column>

                    <!-- Info Column -->
                    <Column width="10rem">
                        <Text text="Amount"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem vAlign="Middle">

                        <cells>
                            <!-- Title -->
                            <Text text="{permitNo}" />
                            <ObjectIdentifier title="{EntryFormDataSourceModel>title}" />

                            <!-- Description -->
                            <Text text="{EntryFormDataSourceModel>desc}" />

                            <!-- Info -->
                            <ObjectNumber number="{EntryFormDataSourceModel>info}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
            <!-- <List id="accessList" mode="MultiSelect" headerText="Most Frequent Accessories" selectionChange="onSelectionChange" items="{
                path: '/AccessPinned_One',
                filters: [{ path: 'pinned', operator: 'EQ', value1: true }]
            }">
                <items>
                    <StandardListItem title="{title}" description="{desc}" info="{info}" infoStateInverted="true"/>
                </items>
            </List>

            <List id="mainAccessList" mode="MultiSelect" headerText="Other Accessories" selectionChange="onSelectionChange" items="{
                path: '/AccessPinned_Two',
                filters: [{ path: 'pinned', operator: 'NE', value1: true }]
            }">
                <items>
                    <StandardListItem title="{title}" description="{desc}" info="{info}" infoStateInverted="true"/>
                </items>
            </List> -->
        </VBox>
    </HBox>

</core:FragmentDefinition>