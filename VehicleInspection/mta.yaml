_schema-version: 3.3.0
ID: VehicleInspection
description: A simple CAP project.
version: 1.0.0
modules:
  - name: VehicleInspection-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: VehicleInspection-auth
      - name: VehicleInspection-db
      - name: VehicleInspection-connectivity
      - name: VehicleInspection-destination
      - name: VehicleInspection-logging
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      readiness-health-check-http-endpoint: /health
      readiness-health-check-type: http
    build-parameters:
      builder: npm
  - name: VehicleInspection-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: VehicleInspection-db
    parameters:
      buildpack: nodejs_buildpack
  - name: VehicleInspection-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: VehicleInspection-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - adnocvivehicleinspection.zip
          name: adnocvivehicleinspection
          target-path: resources/
  - name: adnocvivehicleinspection
    type: html5
    path: app/vehicleinspection
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: VehicleInspection-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: VehicleInspection-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: VehicleInspection-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: VehicleInspection-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
  - name: VehicleInspection-destination
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
      service: destination
      service-plan: lite
  - name: VehicleInspection-logging
    type: org.cloudfoundry.managed-service
    parameters:
      service: application-logs
      service-plan: standard
  - name: VehicleInspection-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: VehicleInspection-html5-service
      service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
